---
import BaseLayout from "../../layouts/BaseLayout.astro";
import ProductCard from "../../components/ProductCard.astro";
import Breadcrumbs from "../../components/Breadcrumbs.astro";
import { getCollection } from "astro:content";

const repuestos = await getCollection("repuestos");
---

<BaseLayout
  title="Catálogo de repuestos de calderas — Criterio Térmico"
  description="Repuestos originales disponibles: ventiladores, flujostatos Bitron, termostatos táctiles, válvulas y detentores. Retiro en Villa Martelli, Vicente López."
>
  <section class="section">
    <div class="container">
      <Breadcrumbs items={[{ label: "Repuestos" }]} />

      <div class="section-header section-header--left" style="margin-bottom: 44px;">
        <h1>Repuestos originales disponibles</h1>
        <p class="section-header__desc">
          Cada ficha trae el código, los modelos en los que entra y la marca, cuando el repuesto la tiene. Hoy el stock es de repuestos Peisa y kits de conexión genéricos. Retiro en persona en Domingo French 210, Villa Martelli: consultá disponibilidad y precio por WhatsApp.
        </p>
      </div>

      <div class="product-grid">
        {repuestos.map((item) => (
          <ProductCard
            nombre={item.data.nombre}
            codigo={item.data.codigo}
            slug={item.data.slug}
            imagen={item.data.imagen}
            disponible={item.data.disponible}
            modelos_compatibles={item.data.modelos_compatibles}
            whatsapp_mensaje={item.data.whatsapp_mensaje}
          />
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
